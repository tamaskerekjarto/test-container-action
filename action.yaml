name: 'my first container action'
description: 'Actions at their best'
author: 'Tamas'
inputs: 
  myInput:
    description: 'greeting to use'
    required: true
    default: 'Awesome Guru'
outputs:
  myOutput:
    description: 'total memory'
runs:
  using: 'docker'
  image: 'Dockerfile'
